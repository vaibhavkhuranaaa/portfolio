# Portfolio handoff

Read `AGENTS.md`, this file, and `docs/STATE.md`. Use `scripts/project-registry.mjs` as the approval source and inspect only the project manifests or presentation files named by the task.

## Current objective

Keep the portfolio site, GitHub README index, and consumer projections synchronized to the same approved exact project revisions.

## Next action

Run the declared verification commands. If the registry changes, regenerate all consumers, review the exact source revisions, and use the protected Vercel release workflow only after exact-SHA approval.

## Authority

Do not admit an unapproved project, fabricate evidence, bypass an expired demo, expose secrets, or provision paid infrastructure.
