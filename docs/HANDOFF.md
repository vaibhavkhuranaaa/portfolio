# Portfolio handoff

Read `AGENTS.md`, this file, and `docs/STATE.md`. Use `scripts/project-registry.mjs` as generated publication truth and inspect only the release contracts or manifests named by the task.

## Current objective

Keep the portfolio, GitHub README index, and downstream résumé synchronized to anonymously verified live project revisions.

## Next action

Run the declared verification commands. The scheduled workflow discovers enabled live projects, verifies their deployed SHA, commits projections, and lets Vercel's Git integration release `main`.

## Authority

Do not admit a project without an enabled public release contract and matching live SHA, fabricate evidence, bypass an expired demo, expose secrets, or provision new paid infrastructure.
